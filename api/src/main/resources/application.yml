spring:
  datasource:
    url: jdbc:mysql://localhost:3307/perfdb?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: perfuser
    password: perfpass
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
